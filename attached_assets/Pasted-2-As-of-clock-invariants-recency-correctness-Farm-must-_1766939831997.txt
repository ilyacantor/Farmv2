2) As-of clock invariants (recency correctness)

Farm must be able to prove that its recency labeling is coherent.

Checks:

Snapshot has a single generated_at / as_of timestamp, and all observation timestamps are within plausible bounds relative to it (no “future,” no extreme past).

The RECENT/STALE split is deterministic for a fixed recency window and seed.

Fail mode it catches: if Farm itself is inconsistently labeling activity, you can’t blame AOD.

3) Ongoing finance determinism + distribution sanity

Because ongoing finance is at the center of your current FPs.

Checks:

For each asset: finance plane should deterministically generate a finance_status ∈ {NONE, ONE_TIME, ONGOING} (or equivalent).

Expected flags derived from that must match:

ONGOING ⇒ HAS_ONGOING_FINANCE

ONE_TIME ⇒ HAS_FINANCE && !HAS_ONGOING_FINANCE

Distribution sanity (by profile): in an “enterprise messy” snapshot you should not get bizarre extremes (e.g., 95% ongoing, or 0 ongoing).

Fail mode it catches: grading drift in finance semantics.

4) Cross-plane join hygiene audit (no cheating, no silent join keys)

Farm promises planes are independent and only correlate via realistic keys. 

replit (10)

Checks:

No hidden join keys introduced into multiple planes.

Correlation keys are only:

domain / registered domain

vendor name (normalized)

known ids that exist IRL (account ids etc. if appropriate)

Fail mode it catches: “easy mode” creeping into Farm that makes grading meaningless.

5) Gradeability gate is strict

Farm reconciliation should surface “cannot grade” loudly if contract prerequisites are missing. 

replit (10)

Checks:

If AOD output is missing required fields (reason codes empty, classifications missing), Farm sets contract_status = INVALID_INPUT_CONTRACT (not “missed/FP”).

If upstream returns HTML/empty, Farm marks UPSTREAM_ERROR, not “no evidence.” 

replit (10)

Fail mode it catches: false confidence when the evaluation pipeline is actually broken.

6) Metamorphic “grading invariance” tests (best ROI)

These are killer because they don’t require AOD correctness—only Farm stability.

For a fixed snapshot:

Change irrelevant fields (e.g., reorder observations) ⇒ expected classification must not change

Re-run same seed ⇒ identical __expected__

Inject a known stressor (Farm says it always injects stress tests) ⇒ expected block must reflect it every time 

replit (10)

Fail mode it catches: grading nondeterminism, which makes perf work impossible to verify.