Thank you for clarifying. Based on the Governance Contract, here's my proposed fix strategy:

Fix Strategy: Separate Authoritative from Heuristic Governance
Semantic Clarification
Signal	Meaning	Asserts Governance?
HAS_CMDB	Direct CMDB record match (domain/uri/canonical_name)	YES
HAS_IDP	Direct IdP record match (domain/uri/canonical_name)	YES
VENDOR_GOVERNED (new)	Inherited via vendor family propagation	Enrichment only
Changes Required
1. admission.py (where the bug originates)

lens_coverage.cmdb/idp = direct matches ONLY (no propagation)
vendor_governed=True = set when governance propagated from vendor family
Already partially done in my initial patch
2. aod_agent_reconcile.py

Add new ReasonCode.VENDOR_GOVERNED
Emit it when asset.vendor_governed=True
Classification uses: is_governed = has_idp OR has_cmdb OR vendor_governed
But HAS_CMDB/HAS_IDP only emitted for direct matches
3. output_contracts.py

Add VENDOR_GOVERNED to ReasonCode enum
4. Reconciliation report

AOD emits NO_CMDB for outlook.com (correct)
AOD emits VENDOR_GOVERNED for outlook.com (if applicable)
Farm reconciliation accuracy should improve
Expected Outcome
8 CMDB correlation bugs fixed (outlook.com, trello.com, etc.)
3 IdP correlation bugs fixed
12 zombie false positives resolved
Clear audit trail: reason codes distinguish direct vs propagated governance
Does this strategy align with your intended design? Should I proceed with implementation?

Worked for 51 seconds
Your plan is mostly right. Two crisp edits:

Don’t call VENDOR_GOVERNED “enrichment only” if it flips classification.
If it flips classification, it’s not “just context” — it’s an explicit governance rule. That’s fine; just name it honestly.

Make sure admissions and reason codes are consistent:

Admission gates should reference is_governed (including vendor_governed) only if policy says so.

Reason codes must clearly show why: HAS_CMDB vs VENDOR_GOVERNED.

Answer to “Should I proceed?”

Yes — proceed with this separation:

HAS_CMDB/HAS_IDP = direct authoritative only

Vendor propagation = VENDOR_GOVERNED flag + reason code

Classification may include vendor_governed, but never label it as HAS_CMDB/HAS_IDP

One more requirement: Farm and AOD must use the same semantics, or you’ll chase ghosts forever. If Farm’s oracle currently treats vendor propagation as “HAS_CMDB,” then Farm must be updated to output VENDOR_GOVERNED too (or at least expose it distinctly in the expected block).