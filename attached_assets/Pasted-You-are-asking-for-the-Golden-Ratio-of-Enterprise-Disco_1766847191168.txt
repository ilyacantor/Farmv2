You are asking for the **"Golden Ratio" of Enterprise Discovery**. Based on industry benchmarks (like what you'd see from a Netskope, Zscaler, or specialized CASB audit), here is the data you need to tune your Farm for realism.

### 1. The Benchmark Data (The Target)

In a real, large-scale enterprise environment, the funnel typically looks like this:

* **Raw Noise (Ingested):** 100% (The firehose).
* **Valid Domains (Validated):** ~5-10% (After filtering ads, CDNs, tracking, and internal hostnames).
* **Business Relevant (Admitted):** ~1-3% of total volume, but **~20-30% of Valid Domains**.

**The Current Problem:** Your funnel is `46,000` -> `266`. That is **0.5%** of the total, and likely a very low percentage of the "Validated" bucket.

**The Fix:** You need to tune the **"Corroboration Rate"**.
In a real company, if everyone uses Slack, you will see it in DNS *and* Endpoint *and* Identity. In your current Farm, you are likely generating "Slack" in DNS, and "Zoom" in Endpoint, but rarely both for the same asset.

### 2. Tuning the Farm: The "Deep Fake" Strategy

To fix the admission rate, we must move from generating "Random Volume" to generating **"Coupled Evidence."**

We need to force the Farm to generate **Multi-Plane Evidence** for the assets we *want* to admit.

#### The "Realistic" Recipe

Update your generator logic to follow these distribution rules:

1. **The "Core Stack" (Top 50 Apps):**
* **Rule:** Must appear in **3+ Planes** (Identity + Network + Endpoint).
* **Volume:** High.
* **Admission Rate:** 100%.


2. **The "Departmental Ops" (Next 200 Apps):**
* **Rule:** Must appear in **2 Planes** (Network + Endpoint OR Network + Finance).
* **Admission Rate:** 100%.


3. **The "Shadow/Noise" (The Long Tail):**
* **Rule:** Single source only (Network only or Finance only).
* **Admission Rate:** 0% (Correctly rejected by your Gate).



### 3. 🚜 Master Prompt: Implement Multi-Plane Corroboration

**Context:** The current Farm generates mostly single-source evidence, leading to unrealistic rejection rates. Real enterprise assets usually leave footprints in multiple planes (e.g., Network + Endpoint).

**Task:** Refactor `generate_mock_data.py` to implement **"Coupled Evidence Generation."**

**1. Create `generate_multi_plane_asset()` function:**

* Instead of generating random rows, generate a complete **"Asset Profile"**:
* Select a Target App (e.g., "Miro").
* **Plane 1 (Network):** Generate 50 DNS logs for `miro.com`.
* **Plane 2 (Endpoint):** Generate 10 Process logs for `Miro.exe`.
* **Plane 3 (Finance):** (Optional) Generate 1 Invoice for "Miro Inc".


* **Loop:** Execute this function for `N` target assets (e.g., 300 times).

**2. Adjust Probability Weights:**

* **Core Assets (high priority):** 100% chance of Multi-Plane.
* **Shadow Assets (medium priority):** 40% chance of Multi-Plane (to simulate "advanced shadow").
* **Noise Assets (low priority):** 0% chance (Single source only).

**3. Verification:**

* Set `TARGET_ADMITTED_COUNT = 400`.
* Run generation.
* **ASSERT** that the final Admission Count is close to 400. (This proves the Multi-Plane logic successfully bypassed the Source Diversity Gate).

---

**Why this works:**
It creates a **"Populated"** feel. You aren't just seeing random noise; you are seeing a simulated company where people actually *use* software across different devices and networks. This will drive your admitted count up to the realistic ~400 range without lowering your strict admission standards.