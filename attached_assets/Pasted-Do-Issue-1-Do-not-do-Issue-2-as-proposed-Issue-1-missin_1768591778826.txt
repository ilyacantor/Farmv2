Do **Issue 1**. Do **not** do **Issue 2** (as proposed).

### ✅ Issue 1 (missing `canonical_domain`) — proceed

That’s pure CMDB data quality. If stress/zombie CMDB rows omit `canonical_domain`, correlation will fail even when everything else is right. Add it.

* Add `canonical_domain=toxic["domain"]`
* Add `canonical_domain=zombie["domain"]`

No semantic risk. This is a real bugfix.

---

### ❌ Issue 2 (apply alias collapse to CMDB zombie entries) — don’t do it

This *is* a semantic change and it will lock you into the wrong model.

Why:

* The reconciliation report explicitly shows **KEY_NORMALIZATION_MISMATCH** for `hipchat.com` and `yammer.com` (and `zoom-legacy.com`) because **Farm expects them standalone keys** while AOD is collapsing. 
* If you “fix” Farm by collapsing CMDB to vendor keys, you erase the ability to treat legacy products as **distinct zombies** and you’ll bake in “vendor collapse = truth,” which is not always correct.

**Correct direction:** keep Farm CMDB as **truthful domain keys**, and make AOD’s key selection treat legacy products as standalone (or represent lineage via metadata instead of key collapse). That’s the clean separation.

So: **do not mutate Farm’s CMDB keys to match AOD’s collapse behavior.**

---

## What Farm should do instead for Issue 2

If you want to reduce noisy mismatches without changing semantics:

* Add an explicit metadata field on those CMDB entries:

  * `alias_of: atlassian.com` for hipchat
  * `alias_of: microsoft.com` for yammer
    (or whatever your schema equivalent is)
* Keep `canonical_domain` as the actual domain (`hipchat.com`, `yammer.com`)

This preserves history and still enables a UI “owned by” linkage later.

---

## Bottom line

* **Proceed with Issue 1 now.**
* **Hold Issue 2** until the AOD/Farm contract is settled: legacy product domains should remain standalone keys (my recommendation), and AOD should stop collapsing them.

If you want, I’ll write the exact Replit prompt for Farm to implement Issue 1 only.
