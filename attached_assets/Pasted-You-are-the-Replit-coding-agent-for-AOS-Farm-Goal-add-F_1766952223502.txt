You are the Replit coding agent for AOS Farm. Goal: add Farm-level grading correctness checks so we can trust evaluations while AOD is changing for performance. No bandaids, no “green-test theater.” Follow Farm’s DONE rules and Fix Proposal Format.

CONTEXT
- We have the SAME snapshot (id prefix: ed6faf6a…) but different runs show slightly different totals.
- We also see systematic disagreements like Farm=HAS_ONGOING_FINANCE while AOD=NO_ONGOING_FINANCE, and Farm=RECENT while AOD=STALE.
- Before touching AOD logic, we must prove Farm grading is deterministic and grounded in signals that AOD can actually see.

DELIVERABLES (must ship all)
1) A new “Grading Correctness” audit suite that runs on a snapshot id and produces a PASS/FAIL report.
2) At least 3 negative tests ensuring failures become explicit contract statuses (UPSTREAM_ERROR / INVALID_INPUT_CONTRACT / INVALID_SNAPSHOT), not silent “clean/missed.”
3) Proof: one real run on the ed6faf6a… snapshot producing an audit report + one failure-mode demo (forced non-JSON or missing required fields).

TASKS

A) Determinism / Idempotency audit (highest priority)
Implement an audit function/endpoint/CLI (choose simplest in this repo) that:
- Loads the exact snapshot by snapshot_id (no regeneration).
- Runs the Farm expected-block grading computation N times (N=10) in-process.
- Asserts identical results across runs:
  - identical counts per category (shadow/zombie/clean/parked/not_admitted as applicable)
  - identical canonical asset key sets per category
  - identical per-asset reason code sets (order-insensitive)
If any mismatch occurs:
- FAIL the audit with a diff:
  - which keys moved categories
  - which reason codes changed
- Mark snapshot contract_status = INVALID_SNAPSHOT (or produce an explicit audit failure object) — do NOT silently proceed.

B) Expected-block self-consistency audit
On the snapshot’s __expected__ block (or whatever expected structure exists), assert:
- Every expected asset has non-empty reason codes.
- Mutually exclusive flags are not both present:
  - RECENT vs STALE
  - HAS_IDP vs NO_IDP
  - HAS_CMDB vs NO_CMDB
- Implication rules:
  - HAS_ONGOING_FINANCE => HAS_FINANCE
If violations exist:
- FAIL loudly (INVALID_SNAPSHOT) with a list of violating assets and which invariants failed.

C) Finance anchoring traceability audit (grounding check)
We must prove Farm’s HAS_ONGOING_FINANCE is derived from concrete evidence refs and can be mapped to what AOD consumes.
Implement:
- For a configurable list of asset keys (default list: zapier.com, airtable.com, hubspot.com, figma.com, canva.com):
  - Print/return:
    - expected finance status (NONE / ONE_TIME / ONGOING)
    - the exact evidence_ref(s) or finance-plane record ids that caused HAS_ONGOING_FINANCE
- Add a check: if HAS_ONGOING_FINANCE is true but there are zero concrete supporting refs/records, FAIL (INVALID_SNAPSHOT).
This is NOT about changing semantics; it’s about making grading explainable and auditable.

D) Activity “as-of” invariants audit
Ensure activity recency labeling is coherent relative to snapshot meta generated_at/as_of:
- Verify no activity timestamps are in the future relative to as_of.
- Verify RECENT/STALE classification is reproducible given the configured activity_window_days.
- Add a tiny golden fixture test set (6 assets) with known timestamps around the boundary to validate recent/stale/none behavior.

E) Reconciliation gradeability enforcement
Add strict checks in the reconciliation pipeline (Farm side):
- If AOD response is non-JSON or content-type not JSON => contract_status = UPSTREAM_ERROR (do not treat as “no evidence”).
- If AOD output is missing required fields (classification label or reason codes) => contract_status = INVALID_INPUT_CONTRACT.
- Do not convert these into missed/FP metrics.

IMPLEMENTATION NOTES
- Choose the simplest integration point:
  - either a CLI: `python -m farm.audit --snapshot_id ...`
  - or a FastAPI endpoint: `/api/audit/grading?snapshot_id=...`
- Keep audits fast and deterministic.
- Do not add “fixup” code that mutates the snapshot to pass audits. Audits are read-only.

PROOF REQUIREMENTS
- Run the audit against snapshot ed6faf6a… and show PASS with stable hashes/sets across N=10.
- Demonstrate one forced failure:
  - simulate non-JSON AOD response OR remove a required field from a mocked response
  - show Farm marks UPSTREAM_ERROR or INVALID_INPUT_CONTRACT.
- Output must follow Farm Fix Proposal Format exactly:
  1) What broke
  2) Why it broke
  3) What I changed
  4) Why this is IRL-correct
  5) Tempting cheat avoided
  6) How I proved it (tests + one real run audit output)

STOP CONDITION
If you cannot prove determinism (audit fails), stop further work and report the exact diff. Do not proceed to “improve grading” until determinism is restored.
