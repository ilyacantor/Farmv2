You are the CTO + lead engineer for AutonomOS (AOS). You will implement a robust, production-grade fix in the AOS Farm repo (Replit) for this issue:

**Current issue:** Supabase Postgres *session pooler* is temporarily saturated due to rapid restart attempts during debugging (connection/retry storm).  
**Constraint:** We cannot use direct Postgres due to IPv4/IPv6 reachability. We MUST use Supabase pooler.  
**Stack:** FastAPI + Uvicorn + asyncpg + Pydantic + Jinja2 + Vanilla JS/Tailwind. Running on Replit.

## Goals (non-negotiable)
1) Stop the connection storm and prevent re-saturating the pooler.
2) Ensure Farm remains usable even when DB is temporarily unavailable (graceful degradation).
3) Centralize DB management: *one global asyncpg pool per process* with strict limits.
4) Add circuit breaker + exponential backoff on pool creation and acquisition errors.
5) Cap DB parallelism in application code (semaphore), not by enlarging pools.
6) Make Uvicorn run single-process in Replit debug mode (workers=1).
7) No “double pooling” and no per-request pool creation.
8) Provide clear logs for DB state: healthy / retrying / cooldown.
9) Provide a simple env-var “knob panel” for tuning without code changes.

## Deliverables
A) New module `src/farm/db.py` (or appropriate existing folder) that implements:
   - Singleton `asyncpg.Pool` creation with conservative settings for Supabase pooler
   - Exponential backoff on creation failures: 10s → 20s → 40s → 80s… capped at 120s
   - Circuit breaker:
        - After N failures (default 8) enter cooldown (default 180s)
        - During cooldown, DB calls fail fast with 503-friendly exception
   - Concurrency guard: global `asyncio.Semaphore` around DB-heavy operations (default 2)
   - `close_pool()` on app shutdown
   - Turn off statement caching (statement_cache_size=0)
   - Tiny pool sizes: min_size=0, max_size=2 by default
   - Aggressive release: max_inactive_connection_lifetime=10

B) Wire it into FastAPI:
   - Register startup/shutdown hooks
   - Add exception handler mapping `DBUnavailable` to HTTP 503 with actionable message
   - Update all DB usage to go through a single helper:
        - `await db.with_db(fn, *args, **kwargs)` OR
        - `async with db.connection() as conn: ...`
     Choose one consistent pattern and refactor all call sites.

C) Add “degraded mode” behavior:
   - If DB is down, key pages/APIs return 503 with a short message:
     “DB pooler saturated; Farm is backing off. Try again in ~X seconds.”
   - Static UI should still load. Pages that require DB should show a banner/message.

D) Replit run configuration:
   - Ensure Uvicorn runs with `--workers 1` in Replit so we don’t spawn multiple pools.
   - If there is a Procfile / .replit / run command, update it accordingly.

E) Add env vars + defaults (document in README or `docs/ops_db.md`):
   - DB_POOL_MIN (default 0)
   - DB_POOL_MAX (default 2)
   - DB_CONNECT_TIMEOUT (default 10)
   - DB_COMMAND_TIMEOUT (default 15)
   - DB_MAX_INACTIVE_LIFETIME (default 10)
   - DB_BACKOFF_BASE (default 10)
   - DB_BACKOFF_CAP (default 120)
   - DB_FAIL_THRESHOLD (default 8)
   - DB_COOLDOWN_SECONDS (default 180)
   - DB_CONCURRENCY (default 2)

F) Safety checks + tests (must be runnable in repo):
   - Add a minimal test or diagnostic script that simulates DB-unavailable:
        - Temporarily point DATABASE_URL to an invalid host OR
        - Add env var `DB_SIMULATE_DOWN=true` in db.py to force failures
     Verify:
        - Backoff triggers
        - Circuit breaker cooldown triggers
        - Endpoints return 503 not 500
   - Ensure no infinite restart loops and no unbounded retries.

## Implementation Notes (important)
- Supabase pooler already pools connections. App-level pool must be tiny.
- Avoid long-lived transactions and “idle in transaction”.
- Ensure we do NOT create a new pool per request.
- Ensure background tasks and ingestion pipelines do not fan out uncontrolled DB writes.
- Prefer structured logging (even basic print with prefixes is fine) but must include:
    - pool creation attempt + delay
    - entering cooldown + duration
    - exiting cooldown (first successful connect)
- Respect project structure; do not invent new frameworks.

## Step-by-step execution plan (you must follow)
1) Locate current DB initialization and every place that creates asyncpg connections/pools.
2) Implement `db.py` singleton pool + circuit breaker + semaphore.
3) Refactor DB call sites to use the singleton helper (no direct asyncpg.connect).
4) Add FastAPI hooks and exception handler.
5) Update Replit run command to `--workers 1`.
6) Add docs + tuning env vars.
7) Add a simple “simulate down” diagnostic and verify behaviors.
8) Provide a short summary of changed files and how to run.

## Acceptance Criteria
- With DB unavailable: Farm returns 503 quickly, does NOT spin/retry aggressively, and enters cooldown after threshold.
- With DB available: normal functionality continues; performance is not worse.
- Restarting Farm repeatedly does NOT saturate pooler as quickly (no storm).
- Only one pool exists per process; pool sizes default to max 2.
- Uvicorn is configured to 1 worker in Replit.

Now implement. Do not ask me questions unless you are blocked by missing file paths; in that case, inspect the repo and decide the correct locations yourself.
