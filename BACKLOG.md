# AOS-Farm Backlog

## Next Farm Pass — Dimension Enum Generation

### Priority: P1 (blocks DCL catalog completeness)

The following DCL dimensions need synthetic allowed_values generated by Farm.
They are flagged `NEEDS_FARM_AUDIT` in `AOS-DCLv2/backend/config/definitions/entities.yaml`.

| Dimension | Context | Expected Output |
|-----------|---------|-----------------|
| `category` | Used by Zendesk support tickets. Needs realistic support ticket categories. | 5-8 enum values (e.g., "Billing", "Technical", "Account", "Feature Request", "Integration", "Onboarding") |
| `training_type` | Used by Workday training_hours metric. Needs realistic corporate training categories. | 4-6 enum values (e.g., "Technical", "Leadership", "Compliance", "Onboarding", "Product") |

### Action Required
1. Add `category` and `training_type` to the Zendesk and Workday pipe schemas respectively
2. Generate synthetic allowed_values for both dimensions
3. Push updated enum values to DCL via the standard Farm→DCL contract update process
4. Remove `NEEDS_FARM_AUDIT` flags from `entities.yaml` once values are populated

### Origin
Identified during DCL Catalog Expansion Pass (2026-02-24). See `farm_dcl_contract.md` Section 5 for existing dimension enums.
